{% extends 'base.html.twig' %}

{% block title %}
	S'inscrire
{% endblock %}

{% block body %}
	<div class="container login">
		<h2>Créer un compte</h2>
		<div class="list">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<button class="btn active">S'inscrire</button>
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
